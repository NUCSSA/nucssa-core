export const rootURL = window.core_admin_data.root_url;
export const restURL = window.core_admin_data.rest_url;
export const ldapConfigRestURL = window.core_admin_data.ldap_config_rest_url;
export const permissionsRestURL = window.core_admin_data.permissions_rest_url;
export const nonce   = window.core_admin_data.nonce;